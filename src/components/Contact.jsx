import ContactVisuals from './ContactVisuals';
import SectionCard from './SectionCard';

export default function Contact() {
  return (
    <SectionCard id="contact" title="Get In Touch" description="Find me on these platforms.">
      <ContactVisuals />
    </SectionCard>
  );
}
